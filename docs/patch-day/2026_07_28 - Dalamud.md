# Dalamud Review — 2026-07-28 — 15.0.2.2 → 15.0.3.0

## Old baseline

- Previous review: 2026-07-28 06:08 -06:00, external report covering `15.0.2.2...15.0.2.3`
  (imported at `findings\Public\Dalamud v15.0.2.2 to v15.0.2.3 - Public Report.md`).
- Previous reviewed tag: `15.0.2.2` / `4a6abae2` (2026-06-23).
- No rolling registry existed before this run (first run of the installed skill);
  the imported report is the only prior local baseline evidence. Confidence in the
  15.0.2.2→15.0.2.3 sub-range rests on that report plus live verification of its
  compare metadata (24 commits / 26 files — confirmed against GitHub today).
- Target API level: 15 on all four plugins.

## Current official state (verified live 2026-07-28)

- **Latest runtime tag: `15.0.3.0`, tagged TODAY (2026-07-28)** — newer than the
  imported report's endpoint. The morning report is already one tag behind.
- Tag history: 15.0.2.2 (Jun 23) → 15.0.2.3 (Jul 17) → 15.0.3.0 (Jul 28).
- NuGet `Dalamud.NET.Sdk` highest: `15.0.0` (unchanged). NuGet `DalamudPackager`
  highest: `15.0.0` (unchanged). **Do not change any SDK pin.**

## Delta A: 15.0.2.2 → 15.0.2.3 (per imported report, metadata re-verified)

24 commits / 26 files. Headlines: `ConfigChangeEvent.Name` added; copied
SigScanner reads `.text` directly; SeString sheet-name cache; drag-drop stale
path clear; BIOS crash diagnostics; libdeflate SqPack fix; ClientStructs
`82f00f3` → `0ce3f022` (88 commits / 133 files); Lumina 7.5.0 → 7.6.0 with seven
changed sheets (Adventure, Item, LegacyQuest, MKDGrowDataSJob, ModelChara,
SatisfactionNpc, WKSScoreList). See the imported public report for detail.

## Delta B: 15.0.2.3 → 15.0.3.0 (collected live via GitHub API)

77 commits / 73 files. Merged PRs: #2857, #2867, #2878, #2879, #2885, #2886,
#2888, #2889, #2890, #2891, #2892, #2893, #2894, #2896, #2897, #2898, #2899,
#2900, #2901. The imported report's post-tag watch items are now IN the runtime:

1. **Shutdown/unload rework** (#2857, #2896, #2897 and related): services
   unregister/cancel via `UnloadCancellationToken`; texture release thread and
   font builds cancel on shutdown; `PluginManager.Dispose` unloads plugins;
   framework destroy hook made global against a race; manual Dalamud unload
   fixed. Affects every plugin's load/unload/reload lifecycle.
2. **SafetyHook hook backend added** (#2892) — new safetyhook-based backend
   "designed to replace Reloaded" — plus **goatcorp.Reloaded.Hooks bump** for
   allocator changes (#2898) and a new address-space data widget. Native hook
   consumers must re-smoke hook creation.
3. **Nullable reference types enabled for all Dalamud projects** (was
   annotations-only). API surface nullability annotations may shift; plugin
   rebuilds can surface new nullability warnings.
4. **New API: `IFramework.CreateDebouncer` / `IDebouncer`** (additive).
5. **`IAsyncDalamudPlugin.LoadAsync` now receives a cancellation token**;
   `IServiceScope`/`IServiceProvider` deadlock fix (#2888).
6. **UnlockState**: update debounced, `IsItemUnlocked` skips non-unlockable
   items, and an **Occult Crescent state null-guard** was added upstream —
   direct evidence the occult native surface is churning.
7. Managed wrapper types `Dalamud/Game/ClientState/Objects/Types/Character.cs`
   and `GameObject.cs` modified (additive members riding the CS target-data
   changes). `IDragDropManager` and `IDalamudAssetManager` touched.
8. Internal: DalamudAtkTweaks split into files; NounProcessor cleanup; env-var
   cleanup; decompression error logging.

### Dependency pins across Delta B

- FFXIVClientStructs: `0ce3f022` → `c8d20f04` → … → `cc474ca9` (three update
  PRs incl. #2899/#2901). Cumulative `0ce3f022...cc474ca9`: **138 commits /
  113 files**. High-signal for us: `Client/Game/Character/Character.cs`,
  `CharacterData.cs`, `DrawDataContainer.cs`, `VfxContainer.cs`,
  `Client/Game/Object/GameObject.cs`, `GameMain.cs`, `UI/PlayerState.cs`,
  `UI/UIState.cs`, `InstanceContent/ContentDirector.cs`,
  `Fate/FateManager.cs`, `Enums/TerritoryIntendedUse.cs`,
  `Component/GUI/AtkUnitBase.cs` + broad Atk*/agent/graphics/file-system
  surfaces.
- Lumina.Excel: `011ca179` → `2b285467` (10 submodule-update commits). Lumina
  **package stays 7.6.0** (verified in `Directory.Packages.props` @ 15.0.3.0).
- EXDSchema experimental: `99040ee2` → `cf037c37` — 11 commits / 51 schema
  files. Notable for us: `InstanceContent`, `SpecialShop`, `Ornament`,
  `MountCustomize`, `NotoriousMonster`, `Adventure`, WKS sheets, Phantom-weapon
  sheets. **`Item` is NOT in this batch** (its change was in Delta A).

## Runtime-tag requirements (15.0.3.0)

No breaking managed-API migration. API 15 intact; SDK/packager unchanged.
Requirements are: rebuild every plugin against the updated dev libs, then
targeted smoke per the impact files — with extra weight on (a) native hook
creation (Reloaded allocator/SafetyHook), (b) unload/reload cycles (shutdown
rework), (c) Character/GameObject/Atk native reads (CS pin), (d) warning-count
baselines (nullable-everywhere).

## Post-tag watch items

Master activity after `15.0.3.0` was not reviewed (tag is 0 days old). Open PRs
were not promoted into requirements. Next run: check post-`15.0.3.0` master.

## Plugin impact

See `findings\Impacts\15.0.2.2 to 15.0.3.0\` — README, Impact Matrix, and one
impact file per plugin (Daedalus, SealBreaker, Charon, Caduceus).
