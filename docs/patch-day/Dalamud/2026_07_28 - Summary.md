# Dalamud Durable Summary — 2026-07-28

Range reviewed: `15.0.2.2` → `15.0.3.0` (endpoint tagged the same day as this
review). First run of the installed `check-dalamud-updates` skill; registry,
findings, and impact structure created under `docs\patch-day\`.

**What changed:** runtime + native bindings + schemas + lifecycle, not the
plugin SDK. API 15 holds; `Dalamud.NET.Sdk/15.0.0` stays. The big movers:
FFXIVClientStructs advanced 226 commits (Character family, GameObject,
PlayerState/UIState, ContentDirector, Atk surfaces), Lumina hit 7.6.0 with a
changed `Item` schema, Dalamud reworked its shutdown/unload path, and the hook
backend gained SafetyHook with a Reloaded allocator bump. New additive APIs:
`ConfigChangeEvent.Name`, `IFramework.CreateDebouncer`.

**What it means for the fleet:** zero code migrations; everything is
rebuild + targeted smoke. Priority: Charon (raw `Character*` casts + mount
rider reads on a twice-changed struct family), then Daedalus (three native
hooks under a changed hook backend, widest CS read surface, 18-warning
baseline vs nullable-everywhere, reload-heavy workflow on the reworked
shutdown path), then SealBreaker (manual Dalamud refs must not drift stale;
addon-listener registry), then Caduceus (rebuild only).

Details: `..\2026_07_28 - Dalamud.md` and
`..\findings\Impacts\15.0.2.2 to 15.0.3.0\`.
