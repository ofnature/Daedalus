name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Download Dalamud
        run: |
          wget -q https://goatcorp.github.io/dalamud-distrib/stg/latest.zip -O dalamud.zip
          unzip -q dalamud.zip -d ~/dalamud

      - name: Restore
        run: dotnet restore Daedalus.sln

      - name: Build
        run: dotnet build Daedalus.sln -c Release --no-restore -p:DalamudLibPath=$HOME/dalamud/

      - name: Create Release Archive
        run: |
          # The asset MUST stay named latest.zip: every release since v0.1.x has used that
          # name and repo.json's DownloadLink* point straight at it. Renaming it silently
          # breaks installs for everyone already on the feed.
          #
          # Find it rather than hardcoding the path — DalamudPackager's output directory
          # moves with the build platform (a solution build can land it under
          # bin/x64/Release/), so a fixed path fails the first time that changes.
          ZIP=$(find Daedalus/bin -name latest.zip -path '*Release*' | head -1)
          if [ -z "$ZIP" ]; then
            echo "ERROR: DalamudPackager produced no latest.zip" >&2
            find Daedalus/bin -type f -name '*.zip' | head -20 >&2
            exit 1
          fi
          echo "Packaged: $ZIP"
          cp "$ZIP" latest.zip

      - name: Extract Version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Extract Patch Notes
        id: changelog
        run: |
          # Extract content between LATEST-START and LATEST-END markers
          # Use heredoc syntax to preserve newlines without URL encoding
          {
            echo 'notes<<EOF'
            sed -n '/<!-- LATEST-START -->/,/<!-- LATEST-END -->/p' Daedalus/CHANGELOG.md | sed '1d;$d' | sed 's/^## .*//'
            echo 'EOF'
          } >> $GITHUB_OUTPUT

      - name: Update Daedalus entry in aggregator repo.json
        run: |
          # repo.json is the shared aggregator manifest (Daedalus + Charon).
          # Update ONLY the Daedalus entry; preserve all other plugins.
          NEW_VERSION="${{ steps.version.outputs.version }}"
          TAG="${{ github.ref_name }}"
          ZIP_URL="https://github.com/ofnature/Daedalus/releases/download/${TAG}/latest.zip"
          DAEDALUS_API=$(jq -r '.DalamudApiLevel' Daedalus/Daedalus.json)
          # Tags are 3-part (vX.Y.Z); Dalamud.NET.Sdk auto-pads the bundled
          # manifest's AssemblyVersion to 4-part (X.Y.Z.0). repo.json must use
          # the 4-part form so Dalamud's strict manifest verification passes.
          ASSEMBLY_VERSION="${NEW_VERSION}.0"
          jq --arg v "$ASSEMBLY_VERSION" --arg url "$ZIP_URL" --argjson api "$DAEDALUS_API" '
            map(
              if .InternalName == "Daedalus" then
                .AssemblyVersion = $v
                | .DalamudApiLevel = $api
                | .DownloadLinkInstall = $url
                | .DownloadLinkTesting = $url
                | .DownloadLinkUpdate = $url
              else . end
            )
          ' repo.json > repo.json.tmp
          mv repo.json.tmp repo.json
          # Sanity check — ensure the Daedalus entry was actually updated.
          if ! jq -e --arg v "$ASSEMBLY_VERSION" 'any(.[]; .InternalName == "Daedalus" and .AssemblyVersion == $v)' repo.json > /dev/null; then
            echo "ERROR: Daedalus entry missing or version not updated in repo.json" >&2
            cat repo.json >&2
            exit 1
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: latest.zip
          generate_release_notes: true

      - name: Commit repo.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout main
          git add repo.json
          if git diff --cached --quiet; then
            echo "repo.json unchanged, skipping commit"
          else
            git commit -m "Update repo.json for ${{ github.ref_name }}"
            git push origin main
          fi

      - name: Discord Notification
        if: success()
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          nodetail: true
          title: "Daedalus ${{ github.ref_name }}"
          description: |
            ${{ steps.changelog.outputs.notes }}

            **[Download from GitHub](https://github.com/ofnature/Daedalus/releases/tag/${{ github.ref_name }})** • Refresh your plugin list to update
          color: 0x5865F2
